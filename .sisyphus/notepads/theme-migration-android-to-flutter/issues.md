# Issues

- Dart/Flutter LSP diagnostics are not configured in this OpenCode environment (`lsp_diagnostics(".")` fails with "No LSP server configured"). Use `flutter analyze` as the static-analysis gate instead.
